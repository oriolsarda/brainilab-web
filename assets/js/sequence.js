/* BrainiLab Sequence — V41.8.0 */
window.BrainiSequence=(function(){
  const ROUNDS=10;const PARAMS=new URLSearchParams(location.search),dailyDate=PARAMS.get("daily")||PARAMS.get("archive")||null,archiveMode=!!PARAMS.get("archive"),dailyMode=!!dailyDate,scoringDaily=dailyMode&&!archiveMode;
  function client(){return window.BrainiBackendAuth?.getClient?.()||null}function configured(){return !!window.BrainiBackendAuth?.isConfigured?.()}function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}function shuffle(a){a=a.slice();for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a}function seedHash(v){let h=2166136261;for(const c of String(v)){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0}
  function localHistory(){try{return BrainiData.anytimeHistory?.("sequence")||{}}catch{return{}}}function excludes(){return Object.entries(localHistory()).filter(([,v])=>Number(v?.timesPlayed||0)>0).map(([id])=>id).filter(id=>/^[0-9a-f-]{36}$/i.test(id)).slice(0,500)}
  function localDaily(){const key=String(dailyDate||"");return (BrainiSequencePuzzles?.all?.()||[]).slice().sort((a,b)=>(seedHash(`${key}:${a.id}`)-seedHash(`${key}:${b.id}`))||String(a.id).localeCompare(String(b.id))).slice(0,ROUNDS).map(x=>({...x,cloud:false,options:shuffle(x.options)}))}
  async function load(){if(configured()){try{const {data,error}=dailyMode?await client().rpc("get_brainilab_daily_sequence",{p_challenge_date:dailyDate}):await client().rpc("get_brainilab_sequence_game",{p_exclude_puzzle_ids:excludes()});if(error)throw error;const rows=(data?.puzzles||[]).map(x=>({id:x.puzzle_id,category:x.category||"math",sequence:(x.sequence||[]).map(Number),options:(x.options||[]).map(Number),cloud:true}));if(rows.length===ROUNDS)return {source:"supabase",rounds:rows}}catch(e){console.warn("Sequence cloud fallback:",e.message||e)}}if(dailyMode)return {source:"local",rounds:localDaily()};const hist=localHistory();const all=shuffle(BrainiSequencePuzzles?.all?.()||[]).sort((a,b)=>(hist[a.id]?.timesPlayed||0)-(hist[b.id]?.timesPlayed||0));return {source:"local",rounds:all.slice(0,ROUNDS).map(x=>({...x,cloud:false,options:shuffle(x.options)}))}}
  async function check(r,value){if(!r.cloud)return {correct:Number(value)===Number(r.answer),answer:r.answer,explanation:r.explanation};const {data,error}=await client().rpc("check_brainilab_sequence_answer",{p_puzzle_id:r.id,p_answer:Number(value)});if(error)throw error;return {correct:!!data.correct,answer:Number(data.answer),explanation:data.explanation||""}}
  async function syncHistory(ids){try{BrainiData.recordAnytimeHistory?.("sequence",ids)}catch{}if(!configured())return;const s=await BrainiBackendAuth?.getSession?.();if(!s?.user)return;const cloud=ids.filter(x=>/^[0-9a-f-]{36}$/i.test(x));if(cloud.length)try{await client().rpc("record_brainilab_sequence_history",{p_puzzle_ids:cloud})}catch(e){console.warn(e)}}
  async function verify(result,details){if(!configured()||result?.cloudSyncStatus!=="synced")return;const s=await BrainiBackendAuth?.getSession?.();if(!s?.user)return;const rows=details.filter(x=>/^[0-9a-f-]{36}$/i.test(x.puzzleId)).map(x=>({puzzle_id:x.puzzleId,answer:x.answer}));if(rows.length!==ROUNDS)return;try{const {data,error}=await client().rpc("verify_brainilab_sequence_result",{p_client_result_id:result.clientResultId,p_rounds:rows});if(error)throw error;await BrainiData.api.markResultAnswerVerified?.(result.clientResultId,data||{})}catch(e){console.warn("Sequence verify:",e.message||e)}}
  async function mount(root){if(scoringDaily && await window.BrainiDailyCompletionGuard?.check?.())return;const intro=root.querySelector("[data-intro]"),loading=root.querySelector("[data-loading]"),stage=root.querySelector("[data-stage]"),resultEl=root.querySelector("[data-result]"),start=root.querySelector("[data-start]"),meta=root.querySelector("[data-meta]"),scoreEl=root.querySelector("[data-score]"),progress=root.querySelector("[data-progress]"),seqEl=root.querySelector("[data-sequence]"),answersEl=root.querySelector("[data-answers]"),feedback=root.querySelector("[data-feedback]"),next=root.querySelector("[data-next]");let rounds=[],idx=0,correct=0,score=0,details=[],started=0,roundStarted=0,source="local",locked=false,finished=false,healthTracker=null;
    function render(){locked=false;roundStarted=performance.now();const r=rounds[idx];healthTracker?.checkpoint(idx+1);meta.textContent=`${dailyMode?(archiveMode?"Past Daily · Practice":"Daily")+" · ":""}Round ${idx+1} of ${ROUNDS}`;scoreEl.textContent=`${score.toLocaleString()} / 2,500`;progress.style.width=`${idx/ROUNDS*100}%`;seqEl.innerHTML=r.sequence.map(n=>`<span>${esc(n)}</span>`).join("")+`<span class="sequence-missing">?</span>`;answersEl.innerHTML="";feedback.textContent="";next.hidden=true;r.options.forEach(v=>{const b=document.createElement("button");b.type="button";b.className="labgame-answer";b.textContent=v;b.onclick=()=>choose(r,v,b);answersEl.appendChild(b)})}
    async function choose(r,v,b){if(locked)return;locked=true;[...answersEl.children].forEach(x=>x.disabled=true);feedback.textContent="Checking…";let x;try{x=await check(r,v)}catch(e){locked=false;[...answersEl.children].forEach(x=>x.disabled=false);feedback.textContent="Could not check that answer.";return}details.push({puzzleId:r.id,answer:Number(v),correct:x.correct,responseTimeMs:Math.round(performance.now()-roundStarted)});[...answersEl.children].forEach(el=>{if(Number(el.textContent)===Number(x.answer))el.classList.add("correct")});if(x.correct){correct++;score+=250;b.classList.add("correct");feedback.innerHTML=`<strong>✓ Correct · +250 pts</strong><span>${esc(x.explanation)}</span>`}else{b.classList.add("wrong");feedback.innerHTML=`<strong style="color:#ff9b96">Not this time</strong><span>${esc(x.explanation)}</span>`}progress.style.width=`${(idx+1)/ROUNDS*100}%`;scoreEl.textContent=`${score.toLocaleString()} / 2,500`;next.textContent=idx===ROUNDS-1?"See result":"Next sequence";next.hidden=false}
    next.onclick=()=>idx===ROUNDS-1?finish():(idx++,render());async function finish(){if(finished)return;finished=true;if(!dailyMode)await syncHistory(details.map(x=>x.puzzleId));healthTracker?.complete(details.map((x,i)=>({contentId:x.puzzleId,position:i+1,attempts:1,isCorrect:x.correct,score:x.correct?250:0,responseTimeMs:x.responseTimeMs})));const timeSec=Math.max(1,Math.round((performance.now()-started)/1000)),accuracy=correct*10;let result=await BrainiData.api.submitGameResult("sequence",{score,correct,total:ROUNDS,accuracy,timeSec,contentSource:source,dailyNumber:scoringDaily?(BrainiData.dailyNumberForDate?.(dailyDate)||null):null,archiveDailyNumber:archiveMode?(BrainiData.dailyNumberForDate?.(dailyDate)||null):null,practice:archiveMode,challengeDate:dailyDate||null});await verify(result,details);stage.hidden=true;resultEl.hidden=false;resultEl.innerHTML=`<div class="labgame-result-inner"><span class="simple-result-kicker">${dailyMode?`${archiveMode?"Past Daily · Practice":"Daily"} #${BrainiData.dailyNumberForDate?.(dailyDate)||""} · `:""}Sequence complete</span><h2>${correct}<small> / ${ROUNDS}</small></h2><p>${score.toLocaleString()} points</p><div class="labgame-result-grid"><div><span>Correct</span><strong>${correct}</strong></div><div><span>Accuracy</span><strong>${accuracy}%</strong></div><div><span>Rounds</span><strong>${ROUNDS}</strong></div></div><div class="inline-quiz-result-actions"><a class="simple-result-play" href="${scoringDaily?"../../daily-quiz/":archiveMode?"../index.html":"./"}">${scoringDaily?"Continue Daily":archiveMode?"Choose another Past Daily game":"Play another set"}</a><a class="simple-result-progress" href="../index.html">Choose another game</a></div></div>`}
    start.onclick=async()=>{start.disabled=true;intro.hidden=true;loading.hidden=false;const x=await load();rounds=x.rounds;source=x.source;loading.hidden=true;if(rounds.length!==ROUNDS){intro.hidden=false;start.disabled=false;root.querySelector("[data-load-error]").hidden=false;return}healthTracker=window.BrainiContentHealth?BrainiContentHealth.create({gameId:"sequence",contentType:"sequence",contentIds:rounds.map(r=>r.id),dailyNumber:scoringDaily?(BrainiData.dailyNumberForDate?.(dailyDate)||null):null}):null;stage.hidden=false;started=performance.now();render()}
  }
  return {mount};
})();
