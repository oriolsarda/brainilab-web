# Supabase Step 23 — V41.5.0

Run `BRAINILAB_STEP23_SQL_COPY_TO_SUPABASE.txt` once in Supabase SQL Editor **after Step 22**.

It adds:
- history-aware Survival loader + result verification;
- Odd One Out content/history/RPCs and 20 starter puzzles;
- Higher or Lower content/history/RPCs and 20 starter pairs;
- Admin list/create/toggle RPCs for the new structured pools;
- one controlled batch-import RPC for structured Content Pool CSVs;
- aggregate Game Analytics for completed cloud plays.

No new Cron is required. Existing Daily scheduling is unchanged.
