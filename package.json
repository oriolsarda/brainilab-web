{
  "name": "brainilab-web",
  "private": true,
  "version": "41.8.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "build": "python3 tools/rebuild-bundles.py && node tools/build.mjs",
    "bundle": "python3 tools/rebuild-bundles.py"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
